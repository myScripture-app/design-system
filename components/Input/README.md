# Input

Text input, textarea, select, checkbox.

- Background: card white (`--card`)
- Border: warm border (`--border-color`), sage focus ring
- Radius: `8px` (`rounded-lg`) — smaller than cards
- Error state: red border + red focus ring

Always use a visible `<label>` — never placeholder-only labels.
